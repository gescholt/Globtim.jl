# GitLab Agent Configuration (Issue #31)
# Optimized settings for Claude Code agents

# API Settings
GITLAB_API_TIMEOUT=60
GITLAB_API_RETRY_COUNT=3
GITLAB_API_RETRY_DELAY=5

# Rate Limiting
GITLAB_API_RATE_LIMIT_DELAY=1
GITLAB_API_BURST_LIMIT=10

# Error Handling
GITLAB_AGENT_FAIL_FAST=true
GITLAB_AGENT_LOG_LEVEL=INFO

# SSH Settings
GITLAB_PREFER_SSH=true
GITLAB_SSH_TIMEOUT=30

# Project Specific
GITLAB_PROJECT_ID=2859
GITLAB_DEFAULT_MILESTONE=""
GITLAB_DEFAULT_LABELS="type::enhancement"

# Agent Behavior
PROJECT_TASK_UPDATER_AUTO_CLOSE=false
PROJECT_TASK_UPDATER_REQUIRE_VALIDATION=true
HOOK_SYSTEM_GITLAB_UPDATES=true
